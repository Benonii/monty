#include "monty.h"

/**
 * nop - does nothing to the stack
 * @stack: The stack
 * @line_number: The line nop was called on
 *
 * Return: None
 */

void nop(stack_t **stack, unsigned int line_number)
{
	(void) stack;
	(void) line_number;
}
