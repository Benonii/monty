#include "monty.h"

/**
 * push - pushes an item to the top of the stack
 * @stack: the stack
 * @line_number: The line number the push command was on
 *
 * Return: None
 */

void push(stack_t **stack, unsigned int line_number)
{
	(void) stack;
	(void) line_number;

	printf("It worked!... not really\n");

}
